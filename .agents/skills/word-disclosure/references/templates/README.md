# Word Disclosure 模板目录

这里存放按语言整理的模板或范文副本。

目录建议按目标语言组织，例如：

- `zh/`：汉语模板
- `en/`：英语模板
- `de/`：德语模板

`STYLE.md` 仍然负责按语言选择范文；模板放入这里后，在 `STYLE.md` 中用对应语言的路径或双链登记。未明确迁移前，不复制或移动现有 `word/<language>/` 词条，避免同一范文产生两个来源。
